/**
 * Copyright (c) 2012, www.yile.com All rights reserved.
 */
package com.yile.games.doll.net.response.state
{
    import com.yile.framework.net.IResponse;
    import com.yile.games.doll.net.vo.*;
    import com.yile.framework.net.Packet;

    /*@import*/

    /**
     * 请求删除状态返回(8:6)
     */
    public class RemoveStateResponse implements IResponse
    {
        /**
         * 用户ID
         */
        public var userID:uint;

        /**
         * 状态id
         */
        public var stateID:uint;

        public function read(p:Packet):void
        {

            userID = p.readUnsignedInt();
            stateID = p.readUnsignedInt();
        }
    }
}